POSTGRES_DB={{ postgres_db | default('music_sync') }}
POSTGRES_USER={{ postgres_user | default('postgres') }}
POSTGRES_PASSWORD={{ postgres_password | default('postgres') }}
POSTGRES_HOST={{ postgres_host | default('localhost') }}
POSTGRES_PORT={{ postgres_port | default('5432') }}
APP_PORT={{ app_port | default('8888') }}
EXTRA_UFW_PORTS={{ extra_ufw_ports | default('') }}

{{ app_tokens_content | default('') }}
